export default function ({ route, redirect, store }) {
/* 
   if (process.server)  return

  const token = store.state.user.token
  
  if (route.path === '/') {
    if (token) return redirect('/guarantees')
    return
  }
  if (!token) return redirect('/') */
}
